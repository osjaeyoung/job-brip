<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mybatis.user">
    <select id="countByNickname" parameterType="String" resultType="Integer">
        SELECT COUNT(*) FROM users WHERE nickname = #{nickname}
    </select>
    <select id="countByEmail" parameterType="String" resultType="Integer">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>
    
    <!-- 회원가입 쿼리 수정 -->
    <insert id="insertUser" parameterType="Map">
        INSERT INTO users (
            cuid, name, nickname, email, password
        ) VALUES (
            #{cuid}, #{name}, #{nickname}, #{email}, #{password}
        )
    </insert>

    <select id="getUserByEmail" parameterType="String" resultType="Map">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <update id="updatePassword" parameterType="Map">
        UPDATE users 
        SET password = #{password}
        WHERE email = #{email}
    </update> 



    <!-- 프로필 조회 -->
    <select id="getUserProfile" parameterType="string" resultType="map">
        SELECT 
            name,
            nickname,
            phone,
            email,
            birth_date,
            profile_image
        FROM users
        WHERE id = #{userId}
    </select>

    <!-- 프로필 이미지 업데이트 -->
    <update id="updateProfileImage" parameterType="map">
        UPDATE users 
        SET profile_image = #{profileImage}
        WHERE id = #{userId}
    </update>

    <!-- 프로필 정보 업데이트 -->
    <update id="updateUserProfile" parameterType="map">
        UPDATE users
        SET 
            name = #{name},
            nickname = #{nickname},
            phone = #{phone},
            birth_date = #{birthDate}
        WHERE id = #{userId}
    </update>
    <!-- 탈퇴 -->
    <update id="withdrawUser" parameterType="string">
        UPDATE users 
        SET 
            is_deleted = true,
            deleted_at = CURRENT_TIMESTAMP,
            email = CONCAT(email, '_', id, '_withdrawn'),  /* 이메일 재사용 방지 */
            nickname = CONCAT(nickname, '_', id, '_withdrawn')  /* 닉네임 재사용 방지 */
        WHERE id = #{userId}
    </update>
</mapper>
